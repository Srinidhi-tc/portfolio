import WorkSection from "../sections/WorkSection";

// The /work page is the interactive Problem → Decisions → Tradeoffs → Impact
// view (moved here from the old Home page, which has been retired).
export default function Work() {
  return <WorkSection />;
}
